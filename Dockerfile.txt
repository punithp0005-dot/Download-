FROM python:3.11-slim

RUN apt-get update && apt-get install -y \
    ffmpeg \
    wget \
    && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir "yt-dlp>=2024.0.0" edge-tts requests

WORKDIR /app
COPY bot.py .

CMD ["python", "bot.py"]
